server:
  port: 11001

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://193.112.18.235:3306/mall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver

# classpath*  * 表示不止扫描自己的类路径，其他引入的jar 包类路径都一起扫描
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto   # auto：实现自增主键